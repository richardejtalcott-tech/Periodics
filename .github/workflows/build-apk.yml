name: Build Periodic v4 Functional Rebuild
on:
  push:
    branches: [ periodic-v4-functional-rebuild ]
  workflow_dispatch:
concurrency:
  group: periodic-v4-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - run: yes | sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - name: Enforce Java 17
        run: java -version 2>&1 | grep '17'
      - run: chmod +x gradlew
      - name: Clean build, tests and lint
        run: |
          ./gradlew --stop || true
          ./gradlew --no-daemon clean assembleDebug testDebugUnitTest lintDebug
      - uses: actions/upload-artifact@v4
        with:
          name: Periodic-v4-functional-rebuild-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
